---
type: decision
title: Phase 6 passed on the box; F15 found and fixed (PR #40)
description: BRING-UP Phase 6 (Confirm the Bind Agreement) PASSED on the VPS 2026-09-22: rc=2, 'apply-changeset: mutation is disabled
tags: []
timestamp: 2026-09-22T20:03:00
sources: [sessions/daily/2026-09-22.md]
status: canon
promoted_at: 2026-09-22
---

BRING-UP Phase 6 (Confirm the Bind Agreement) PASSED on the VPS 2026-09-22: rc=2, 'apply-changeset: mutation is disabled (kill switch absent or unreadable)', and 'ALLOW POST /v1.55/containers/create' in the proxy journal — on Docker Engine 29.8.1 / API v1.55, NEWER than the 28.0.4 CI measured, running the REAL hermes-agent-claude image, not the CI stand-in. So F9's bind agreement holds on the box, and two items leave the unproven list: the box's own Compose version and the real image. The same session applied F10's layout to the box (store 750 root:hermes, log 2750, spool requests 3770 hermes-broker:hermes) and installed both units for the first time; both run active with the broker's two ExecStartPre checks exiting 0 and no docker group. Also measured on the box, confirming spec §2 directly: the gateway's RELATIVE compose sources resolve to the long /opt/projects/claude_code/... form under sudo (M6), while the absolute variable-derived ones (HERMES_ADS_REPO_DIR, HERMES_SPOOL_DIR) arrive verbatim (M2) — which is precisely why F9's fix works. F15 (fixed, PR #40): hermes-docker-proxy.service execs bin/docker-create-proxy.py DIRECTLY but it shipped 100644, so systemd failed with status=203/EXEC and the broker failed behind it with 'Dependency failed'. Nothing caught it — the F9 integration test starts the proxy as 'python3 <script>' and units.test.py only read the unit TEXT; neither runs systemd. LESSON, the same one F9 taught, arriving twice in one day: a check that never exercises the REAL invocation proves nothing about it. Now pinned by units.test.py::TestExecStartProgramsAreExecutable, which asserts every directly-executed Exec* program is 100755 in the git INDEX (the working tree's mode is a local accident) and carries two anti-vacuity controls. F16 (recorded): migrate-governance.py defaults its root to the CONTAINER path /opt/governance, and README's --bootstrap-logs commands omitted --governance-root, so on the host they addressed the wrong tree; fixed in the README, with a follow-up to audit the other host-side tools for the same container-default pattern. Still open in order: F12 (gates the rehearsal AND the kill switch), F14, F3, F6, F8, F16's audit. The kill switch remains ABSENT and mutation stays disabled.
